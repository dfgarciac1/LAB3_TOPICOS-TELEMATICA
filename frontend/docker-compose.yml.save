version: "3.2"
services:
  front:
    image: front.image:latest
    build: ./
    ports:
    - 3000:80
    expose:
    - "3000"
    restart: always
    environment:
        NODE_ENV: production
        P
        HOST: 0.0.0.0
